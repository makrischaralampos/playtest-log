spring:
  datasource:
    url: jdbc:mysql://localhost:3306/playtest_log?serverTimezone=UTC
    username: playtest
    password: playtest
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8080
